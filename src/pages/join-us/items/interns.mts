import type { JoinUsEntry } from "../_types.mts";

const interns: JoinUsEntry = {
  order: 2,
  slug: "interns",
  title: "Interns 🐥",
  description: "Click here to apply intern",
};

export default interns;
